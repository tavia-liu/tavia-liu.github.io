---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0">
  <channel>
    <title>{{ site.title | xml_escape }}</title>
    <description>{{ site.description | xml_escape }}</description>
    <link>{{ site.url }}</link>
    {% assign items = site.posts | concat: site.research | sort: 'date' | reverse %}
    {% for item in items limit: 20 %}
      {% unless item.visibility == "private" or item.published == false %}
        <item>
          <title>{{ item.title | xml_escape }}</title>
          <description>{{ item.description | default: item.excerpt | strip_html | xml_escape }}</description>
          <link>{{ item.url | absolute_url }}</link>
          <guid>{{ item.url | absolute_url }}</guid>
          <pubDate>{{ item.date | date_to_rfc822 }}</pubDate>
        </item>
      {% endunless %}
    {% endfor %}
  </channel>
</rss>
